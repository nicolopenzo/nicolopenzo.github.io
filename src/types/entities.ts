export type Period = {
    location: string;
    from: string;
    to?: string;
    title: string;
    desc: string;
    link?: string;
};

export type Document = {
    title: string;
    link: string;
};
